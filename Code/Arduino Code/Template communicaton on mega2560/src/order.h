#ifndef ORDER_H
#define ORDER_H

// Define the orders that can be sent and received
enum Order {

  // DOIT ÊTRE EXACTEMENT LA MÊME LIST/ORDRE QUE DANS LE PROG PYTHON

  //ORDRE GÉNÉRAL
    HELLO = 0,
    ALREADY_CONNECTED = 1,
    ERROR = 2,
    RECEIVED = 3,
    FINISHED = 4,

    HOME = 5,
    START = 6,
    STOP = 7,
    SEMI_AUTO = 8,
    SERVO_AVANT_DROIT = 9,
    SERVO_AVANT_GAUCHE = 10,
    SERVO_ARRIERE_DROIT = 11,
    SERVO_ARRIERE_GAUCHE = 12,
    MOTEUR_AVANT_DROIT = 13,
    MOTEUR_AVANT_GAUCHE = 14,
    MOTEUR_ARRIERE_DROIT = 15,
    MOTEUR_ARRIERE_GAUCHE = 16,
    ELECTROAIMANT_AVANT_DROIT = 17,
    ELECTROAIMANT_AVANT_GAUCHE = 18,
    ELECTROAIMANT_ARRIERE_DROIT = 19,
    ELECTROAIMANT_ARRIERE_GAUCHE = 20,
  
};

typedef enum Order Order;

#endif
